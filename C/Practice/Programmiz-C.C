#include <stdio.h>
#include <stdbool.h>

int main () {
    // sentinel-controlled forloop

    /*int count;
    int user_input;
    int total = 0;

        printf("Input number, -1 to end sequence: ");
        scanf("%d", &user_input);

    for (count = 0; user_input != -1; count++) {
        total += user_input;
        printf("Input number, -1 to end sequence: ");
        scanf("%d", &user_input);


    }
    printf("Total Number is: %d", total);

    // While Loop Sentinel Controlled Repettition

    int count = 0, total = 0, userInput;

    printf("Input a number, -1 to end sequence: ");
    scanf("%d", &userInput);

    while (userInput != -1){
        total += userInput;
        printf("Input a number, -1 to end sequence: ");
        scanf("%d", &userInput);
        ++count;

    }
    printf("Total Number: %d", total);*/

    // Do-While Repettiton

    /*char gwapo[6] = "Gwapo";
    int count = 0;
    bool gwapoko;
    int buffer;


    printf("Press 1 if Gwapo ko, press 0 if dili: ");
    scanf("%d", &buffer);
    gwapoko = buffer;

    switch (gwapoko)
    {
    case true:
        do {
            printf("%s ", gwapo);
            count++;
        }
        while (count < 10);
        break;
    
    case false:
        printf("Friends Ondang nata oi!");
        break;

    default:
        printf("Tarunga sad tawn ko bai, lain kay ka...");
        break;
    }*/







    return 0;
}