#include <stdio.h>
#include <string.h>

int main () {

    int count = 0;
    char total
    char userName[100];

    printf("Input names of the students (input -1 to end): ");
    fgets(userName, 100, stdin);
    userName[strlen(userName)-1] = '\0';

    while (userName != -1)
    {
        count++;





    }
    
    
    
    
    return 0;
}