
#include <stdio.h>
#include <stdbool.h>

int main() {
    // && logical operator - checks to see if all the conditions
    // are all TRUE inorder to run the statements
    /*float temp = 50;
    bool sunny = true;
    
    if ( temp >= 0 && temp <= 30 && sunny) {
        printf("The weather is GOOD!\n");
        printf("It is Sunny outside");
    }
    else {
        printf("The wether is BAD!!");
    }*/

    // || logial operator it's to check atleast one condition is true
    //  in order to run the statement

    /*float temp = 1000;
    
    if ( temp <=0 || temp >= 30) {
        printf("The weather is Bad!!");
    }
    else {
        printf("The wether is Good!!");
    }*/

    // != NOT Logical Operator to checks if the condition is not equal

    /*bool sunny = 0;

    if (!sunny) {
        printf("\nIt's Cloudy outside!\n ");

    }
    else {
        printf("\nIt's Sunny outside!\n ");
    }*/

    return 0;
}